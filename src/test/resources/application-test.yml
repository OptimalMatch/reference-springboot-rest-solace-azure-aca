spring:
  application:
    name: solace-service-test
  jms:
    solace:
      host: tcp://localhost:55555
      username: admin
      password: admin
      vpn-name: default

solace:
  queue:
    name: test.queue
    topic: test/topic

logging:
  level:
    com.example.solaceservice: DEBUG
    org.testcontainers: INFO